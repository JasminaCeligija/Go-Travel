package com.example.gotravel.domain.usecases

import com.example.gotravel.common.model.User
import com.example.gotravel.data.DefaultUserRepository

/*
class CreateUserUseCase(private val repository: DefaultUserRepository) :
    SuspendUseCase<User, Boolean> {

    override suspend fun doWork(params: User): Boolean {
        repository.createUser(params)
    }

} */